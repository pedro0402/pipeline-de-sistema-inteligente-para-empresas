# pipeline-de-sistema-inteligente-para-empresas

# Ao clonar o projeto:
`cd backend/`
`python3 -m venv venv`
`source venv/bin/activate`
`pip install -r requirements.txt`
`touch .env`
`nano .env`
`DATABASE_URL=postgresql://postgres:<senha-aqui>@db.veeccbsdvqawldfhikgp.supabase.co:5432/postgres`